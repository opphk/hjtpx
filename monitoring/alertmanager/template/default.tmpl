{{ define "slack.default.title" }}
[{{ .Status | toUpper }}] {{ .GroupLabels.alertname }}
{{ end }}

{{ define "slack.default.text" }}
{{ range .Alerts }}
*Alert:* {{ .Labels.alertname }}
*Severity:* {{ .Labels.severity }}
*Summary:* {{ .Annotations.summary }}
*Description:* {{ .Annotations.description }}
{{ if .Annotations.runbook_url }}
*Runbook:* <{{ .Annotations.runbook_url }}|View Runbook>
{{ end }}
{{ if .Annotations.dashboard_url }}
*Dashboard:* <{{ .Annotations.dashboard_url }}|View Dashboard>
{{ end }}
*Time:* {{ .StartsAt }}
{{ end }}
{{ end }}

{{ define "slack.hjtpx.critical" }}
{{ template "slack.default.title" . }}
{{ template "slack.default.text" . }}
{{ end }}

{{ define "email.default.subject" }}
[{{ .Status | toUpper }}] HJTPX Alert: {{ .GroupLabels.alertname }}
{{ end }}

{{ define "email.default.html" }}
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>HJTPX Alert</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 20px; }
    .alert { border: 1px solid #ddd; padding: 15px; margin: 10px 0; border-radius: 5px; }
    .alert.critical { background-color: #ffebee; border-color: #f44336; }
    .alert.warning { background-color: #fff3e0; border-color: #ff9800; }
    .alert.info { background-color: #e3f2fd; border-color: #2196f3; }
    .alert.resolved { background-color: #e8f5e9; border-color: #4caf50; }
    .label { display: inline-block; background-color: #f5f5f5; padding: 5px 10px; margin: 5px; border-radius: 3px; }
    .label.severity { font-weight: bold; }
    table { border-collapse: collapse; width: 100%; margin: 10px 0; }
    th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
    th { background-color: #f5f5f5; }
    .footer { margin-top: 20px; padding-top: 20px; border-top: 1px solid #ddd; color: #666; }
    .btn { display: inline-block; padding: 10px 20px; background-color: #1976d2; color: white; text-decoration: none; border-radius: 5px; margin: 5px; }
    .btn.resolved { background-color: #4caf50; }
  </style>
</head>
<body>
  <h1>HJTPX Alert Notification</h1>

  <div class="alert {{ .Status }}">
    <h2>{{ .Status | toUpper }}</h2>
    <p><strong>Alert Name:</strong> {{ .GroupLabels.alertname }}</p>
    <p><strong>Cluster:</strong> {{ .GroupLabels.cluster }}</p>
    <p><strong>Service:</strong> {{ .GroupLabels.service }}</p>
    <p><strong>Team:</strong> {{ .GroupLabels.team }}</p>
  </div>

  {{ if gt (len .Alerts.Firing) 0 }}
  <h2>Firing Alerts</h2>
  {{ range .Alerts.Firing }}
  <div class="alert critical">
    <h3>{{ .Labels.alertname }}</h3>
    <p><span class="label severity">{{ .Labels.severity }}</span></p>
    <p><strong>Instance:</strong> {{ .Labels.instance }}</p>
    <p><strong>Summary:</strong> {{ .Annotations.summary }}</p>
    <p><strong>Description:</strong> {{ .Annotations.description }}</p>
    <p><strong>Started:</strong> {{ .StartsAt }}</p>
    {{ if .Annotations.runbook_url }}
    <a href="{{ .Annotations.runbook_url }}" class="btn">View Runbook</a>
    {{ end }}
    {{ if .Annotations.dashboard_url }}
    <a href="{{ .Annotations.dashboard_url }}" class="btn">View Dashboard</a>
    {{ end }}
  </div>
  {{ end }}
  {{ end }}

  {{ if gt (len .Alerts.Resolved) 0 }}
  <h2>Resolved Alerts</h2>
  {{ range .Alerts.Resolved }}
  <div class="alert resolved">
    <h3>{{ .Labels.alertname }}</h3>
    <p><strong>Instance:</strong> {{ .Labels.instance }}</p>
    <p><strong>Duration:</strong> {{ .Duration.String }}</p>
    <p><strong>Resolved:</strong> {{ .EndsAt }}</p>
  </div>
  {{ end }}
  {{ end }}

  <div class="footer">
    <p>This alert was generated by <a href="{{ .ExternalURL }}">Alertmanager</a>.</p>
    <p>If you no longer wish to receive these notifications, please contact your administrator.</p>
  </div>
</body>
</html>
{{ end }}
