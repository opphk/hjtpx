# 最佳实践

## 目录

1. [安全性最佳实践](#安全性最佳实践)
2. [性能最佳实践](#性能最佳实践)
3. [可靠性最佳实践](#可靠性最佳实践)
4. [集成最佳实践](#集成最佳实践)
5. [监控与运维最佳实践](#监控与运维最佳实践)
6. [安全合规最佳实践](#安全合规最佳实践)

---

## 安全性最佳实践

### 1. API密钥管理

**错误做法：**
```javascript
// ❌ 硬编码密钥
const apiKey = 'your-secret-key-here';
```

**正确做法：**
```javascript
// ✅ 使用环境变量
const apiKey = process.env.HJTPX_API_KEY;

// ✅ 使用密钥管理服务
const vault = new VaultClient();
const apiKey = await vault.getSecret('hjtpx/api-key');
```

**建议：**
- 使用环境变量或密钥管理服务（如HashiCorp Vault、AWS Secrets Manager）
- 定期轮换密钥（建议每90天）
- 限制API密钥的使用范围和权限

### 2. 请求签名验证

```go
// 生成请求签名
func signRequest(req *http.Request, secret string) {
    timestamp := time.Now().Unix()
    message := fmt.Sprintf("%s%d%s", req.URL.Path, timestamp, req.Method)
    
    mac := hmac.New(sha256.New, []byte(secret))
    mac.Write([]byte(message))
    signature := hex.EncodeToString(mac.Sum(nil))
    
    req.Header.Set("X-Timestamp", strconv.FormatInt(timestamp, 10))
    req.Header.Set("X-Signature", signature)
}

// 验证签名
func verifySignature(secret, signature, path string, timestamp int64) bool {
    // 检查时间戳（5分钟内有效）
    if time.Now().Unix()-timestamp > 300 {
        return false
    }
    
    message := fmt.Sprintf("%s%d", path, timestamp)
    mac := hmac.New(sha256.New, []byte(secret))
    mac.Write([]byte(message))
    expected := hex.EncodeToString(mac.Sum(nil))
    
    return hmac.Equal([]byte(signature), []byte(expected))
}
```

### 3. 防止重放攻击

```go
type VerifyRequest struct {
    SessionID string `json:"session_id"`
    Token     string `json:"token"`
    Timestamp int64  `json:"timestamp"`
    Nonce     string `json:"nonce"`
}

func (r *VerifyRequest) Validate() bool {
    // 检查时间戳
    if time.Now().Unix()-r.Timestamp > 300 {
        return false
    }
    
    // 检查Nonce是否已使用
    key := fmt.Sprintf("nonce:%s", r.Nonce)
    exists, _ := redis.Exists(key).Result()
    if exists {
        return false
    }
    
    // 存储Nonce，设置过期时间
    redis.Set(key, "1", 5*time.Minute)
    
    return true
}
```

### 4. HTTPS强制使用

```nginx
server {
    listen 80;
    server_name example.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name example.com;
    
    ssl_certificate /etc/ssl/certs/example.com.crt;
    ssl_certificate_key /etc/ssl/private/example.com.key;
    
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
    ssl_prefer_server_ciphers on;
    
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
}
```

### 5. 输入验证与过滤

```javascript
// 使用Joi进行输入验证
const Joi = require('joi');

const schema = Joi.object({
    session_id: Joi.string().required().length(36),
    x: Joi.number().integer().min(0).max(500).required(),
    y: Joi.number().integer().min(0).max(500).required(),
    trajectory: Joi.array().items(
        Joi.object({
            x: Joi.number().integer().required(),
            y: Joi.number().integer().required(),
            t: Joi.number().integer().positive().required()
        })
    ).min(3).max(100)
});

const { error, value } = schema.validate(req.body);
if (error) {
    return res.status(400).json({ code: 400, message: error.details[0].message });
}
```

---

## 性能最佳实践

### 1. 连接池配置

```go
// Go客户端连接池配置
cfg := &captcha.Config{
    MaxIdleConns:   10,
    MaxOpenConns:   100,
    ConnMaxLifetime: 30 * time.Minute,
    ConnMaxIdleTime: 5 * time.Minute,
}

// 数据库连接池
db, err := sql.Open("postgres", dsn)
db.SetMaxOpenConns(200)
db.SetMaxIdleConns(50)
db.SetConnMaxLifetime(30 * time.Minute)

// Redis连接池
redisClient := redis.NewClient(&redis.Options{
    Addr:     "localhost:6379",
    Password: "",
    DB:       0,
    PoolSize: 100,
})
```

### 2. 缓存策略

```go
type CaptchaCache struct {
    cache *redis.Client
}

func (c *CaptchaCache) Get(sessionID string) (*CaptchaResult, error) {
    key := fmt.Sprintf("captcha:result:%s", sessionID)
    data, err := c.cache.Get(key).Bytes()
    if err != nil {
        return nil, err
    }
    
    var result CaptchaResult
    json.Unmarshal(data, &result)
    return &result, nil
}

func (c *CaptchaCache) Set(sessionID string, result *CaptchaResult) error {
    key := fmt.Sprintf("captcha:result:%s", sessionID)
    data, _ := json.Marshal(result)
    return c.cache.Set(key, data, 5*time.Minute).Err()
}
```

### 3. 异步处理

```go
func verifyBatch(sessionIDs []string) {
    var wg sync.WaitGroup
    results := make(chan *VerifyResult, len(sessionIDs))
    
    for _, id := range sessionIDs {
        wg.Add(1)
        go func(sessionID string) {
            defer wg.Done()
            result, _ := client.VerifySliderCaptcha(sessionID, "160")
            results <- result
        }(id)
    }
    
    wg.Wait()
    close(results)
    
    for result := range results {
        // 处理结果
    }
}
```

### 4. 限流保护

```go
type RateLimiter struct {
    mu       sync.Mutex
    requests map[string][]time.Time
    limit    int
    window   time.Duration
}

func (r *RateLimiter) Allow(key string) bool {
    r.mu.Lock()
    defer r.mu.Unlock()
    
    now := time.Now()
    cutoff := now.Add(-r.window)
    
    var valid []time.Time
    for _, t := range r.requests[key] {
        if t.After(cutoff) {
            valid = append(valid, t)
        }
    }
    
    if len(valid) >= r.limit {
        r.requests[key] = valid
        return false
    }
    
    r.requests[key] = append(valid, now)
    return true
}
```

### 5. 数据库优化

```sql
-- 添加索引
CREATE INDEX idx_verification_logs_created_at ON verification_logs(created_at);
CREATE INDEX idx_verification_logs_session_id ON verification_logs(session_id);
CREATE INDEX idx_blacklist_value ON blacklist(value);
CREATE INDEX idx_blacklist_type ON blacklist(type);

-- 配置数据库参数
SET max_connections = 200;
SET shared_buffers = '4GB';
SET effective_cache_size = '12GB';
SET maintenance_work_mem = '512MB';
```

---

## 可靠性最佳实践

### 1. 重试机制

```go
func withRetry(fn func() error, maxRetries int) error {
    var err error
    for i := 0; i < maxRetries; i++ {
        err = fn()
        if err == nil {
            return nil
        }
        
        // 指数退避
        time.Sleep(time.Duration(math.Pow(2, float64(i))) * 100 * time.Millisecond)
    }
    
    return fmt.Errorf("重试%d次后仍然失败: %v", maxRetries, err)
}

// 使用重试
err := withRetry(func() error {
    return client.VerifySliderCaptcha(sessionID, position)
}, 3)
```

### 2. 熔断器模式

```go
type CircuitBreaker struct {
    failures    int
    threshold   int
    timeout     time.Duration
    lastFailure time.Time
    state       string // closed, open, half-open
}

func (cb *CircuitBreaker) Call(fn func() error) error {
    if cb.state == "open" {
        if time.Since(cb.lastFailure) > cb.timeout {
            cb.state = "half-open"
        } else {
            return fmt.Errorf("熔断器开启，拒绝请求")
        }
    }
    
    err := fn()
    if err != nil {
        cb.failures++
        cb.lastFailure = time.Now()
        
        if cb.failures >= cb.threshold {
            cb.state = "open"
        }
        
        return err
    }
    
    cb.failures = 0
    cb.state = "closed"
    return nil
}
```

### 3. 超时处理

```go
// 设置合理的超时
client := &http.Client{
    Timeout: 10 * time.Second,
}

// 使用context控制超时
ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
defer cancel()

req, _ := http.NewRequestWithContext(ctx, "POST", url, body)
resp, err := client.Do(req)
```

### 4. 健康检查

```go
type HealthChecker struct {
    db    *sql.DB
    redis *redis.Client
}

func (h *HealthChecker) Check() error {
    if err := h.db.Ping(); err != nil {
        return fmt.Errorf("数据库连接失败: %w", err)
    }
    
    if err := h.redis.Ping().Err(); err != nil {
        return fmt.Errorf("Redis连接失败: %w", err)
    }
    
    return nil
}

func (h *HealthChecker) ServeHTTP(w http.ResponseWriter, r *http.Request) {
    if err := h.Check(); err != nil {
        w.WriteHeader(http.StatusServiceUnavailable)
        json.NewEncoder(w).Encode(map[string]interface{}{
            "status": "unhealthy",
            "error":  err.Error(),
        })
        return
    }
    
    w.WriteHeader(http.StatusOK)
    json.NewEncoder(w).Encode(map[string]interface{}{
        "status": "healthy",
    })
}
```

---

## 集成最佳实践

### 1. 验证码类型选择

| 场景 | 推荐验证码类型 | 原因 |
|------|--------------|------|
| 登录/注册 | 滑块验证码 | 用户体验好，安全性适中 |
| 高安全场景 | 3D验证码/手势验证码 | 安全性高 |
| 移动端 | Emoji验证码/手势验证码 | 适合触屏操作 |
| 视障用户 | 语音验证码 | 无障碍支持 |
| 游戏场景 | 连连看验证码 | 趣味性强 |

### 2. 验证码难度调整

```javascript
// 根据风险等级调整难度
function selectCaptchaDifficulty(riskScore) {
    if (riskScore >= 70) {
        return 'hard';      // 高风险：3D验证码或手势验证码
    } else if (riskScore >= 40) {
        return 'medium';    // 中风险：滑块验证码
    } else {
        return 'easy';      // 低风险：点击验证码或无感验证
    }
}
```

### 3. 前端集成最佳实践

```javascript
// 初始化验证码组件
const captcha = new CaptchaWidget({
    container: '#captcha-container',
    apiServer: 'https://api.example.com',
    apiKey: process.env.API_KEY,
    
    // 自动刷新
    autoRefresh: true,
    refreshOnExpire: true,
    
    // 错误处理
    onError: (error) => {
        console.error('验证码错误:', error);
        showErrorToast('验证码加载失败，请重试');
    },
    
    // 验证成功
    onSuccess: (result) => {
        // 将token存入表单
        document.getElementById('captcha-token').value = result.token;
    }
});

// 表单提交前检查
form.addEventListener('submit', (e) => {
    if (!captcha.isVerified()) {
        e.preventDefault();
        showErrorToast('请先完成验证码验证');
    }
});
```

### 4. 后端验证最佳实践

```go
func handleLogin(w http.ResponseWriter, r *http.Request) {
    var req LoginRequest
    json.NewDecoder(r.Body).Decode(&req)
    
    // 1. 验证验证码Token
    valid, err := verifyCaptchaToken(req.CaptchaToken)
    if err != nil || !valid {
        http.Error(w, "验证码验证失败", http.StatusBadRequest)
        return
    }
    
    // 2. 检查风险分数
    riskScore := getRiskScore(req.UserID)
    if riskScore > 70 {
        // 高风险，记录日志并可能要求额外验证
        log.Warnf("高风险登录尝试，用户ID: %s，风险分数: %.2f", req.UserID, riskScore)
        
        // 可以要求二次验证
        if !has2FAEnabled(req.UserID) {
            http.Error(w, "请启用双重认证", http.StatusForbidden)
            return
        }
    }
    
    // 3. 执行登录逻辑
    // ...
}
```

### 5. 多语言支持

```javascript
const i18n = {
    'zh-CN': {
        slider: {
            title: '请滑动完成验证',
            loading: '加载中...',
            success: '验证成功',
            fail: '验证失败，请重试'
        }
    },
    'en-US': {
        slider: {
            title: 'Please complete the verification',
            loading: 'Loading...',
            success: 'Verification successful',
            fail: 'Verification failed, please try again'
        }
    },
    'ja-JP': {
        slider: {
            title: 'スライドして認証を完了してください',
            loading: '読み込み中...',
            success: '認証成功',
            fail: '認証に失敗しました、もう一度お試しください'
        }
    }
};

// 使用
const locale = navigator.language;
captcha.setLocale(i18n[locale] || i18n['en-US']);
```

---

## 监控与运维最佳实践

### 1. 指标收集

```go
// 暴露Prometheus指标
var (
    captchaRequestsTotal = prometheus.NewCounterVec(
        prometheus.CounterOpts{
            Name: "captcha_requests_total",
            Help: "验证码请求总数",
        },
        []string{"type", "status"}
    )
    
    captchaLatency = prometheus.NewHistogramVec(
        prometheus.HistogramOpts{
            Name:    "captcha_latency_seconds",
            Help:    "验证码处理延迟",
            Buckets: prometheus.DefBuckets,
        },
        []string{"type"}
    )
    
    verificationSuccessRate = prometheus.NewGauge(
        prometheus.GaugeOpts{
            Name: "captcha_verification_success_rate",
            Help: "验证码验证成功率",
        }
    )
)

func init() {
    prometheus.MustRegister(captchaRequestsTotal)
    prometheus.MustRegister(captchaLatency)
    prometheus.MustRegister(verificationSuccessRate)
}

// 在处理函数中记录指标
func handleCaptchaVerify(w http.ResponseWriter, r *http.Request) {
    start := time.Now()
    
    // 处理请求
    // ...
    
    duration := time.Since(start).Seconds()
    captchaLatency.WithLabelValues("slider").Observe(duration)
    captchaRequestsTotal.WithLabelValues("slider", status).Inc()
}
```

### 2. 日志记录

```go
// 结构化日志
log.WithFields(log.Fields{
    "session_id":   sessionID,
    "user_id":      userID,
    "captcha_type": "slider",
    "result":       "success",
    "risk_score":   15.5,
    "duration_ms":  time.Since(start).Milliseconds(),
}).Info("验证码验证完成")

// 错误日志
log.WithFields(log.Fields{
    "error":        err.Error(),
    "session_id":   sessionID,
    "captcha_type": "slider",
}).Error("验证码验证失败")
```

### 3. 告警配置

```yaml
# Prometheus告警规则
groups:
  - name: captcha
    rules:
      - alert: HighErrorRate
        expr: |
          rate(captcha_requests_total{status="error"}[5m]) /
          rate(captcha_requests_total[5m]) > 0.05
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "验证码错误率过高"
          description: "错误率超过5%"
      
      - alert: HighLatency
        expr: histogram_quantile(0.95, rate(captcha_latency_seconds_bucket[5m])) > 1
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "验证码P95延迟超过1秒"
      
      - alert: ServiceDown
        expr: up{job="hjtpx"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "验证码服务不可用"
      
      - alert: HighMemoryUsage
        expr: (sum(node_memory_MemTotal_bytes - node_memory_MemAvailable_bytes) / sum(node_memory_MemTotal_bytes)) * 100 > 80
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "内存使用率超过80%"
```

### 4. 定期备份

```bash
#!/bin/bash
# backup.sh

BACKUP_DIR="/var/backups/hjtpx"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p $BACKUP_DIR

# 备份PostgreSQL
docker-compose exec -T postgres pg_dump -U postgres hjtpx_db | gzip > $BACKUP_DIR/postgres_$DATE.sql.gz

# 备份Redis
docker cp $(docker-compose ps -q redis):/data/dump.rdb $BACKUP_DIR/redis_$DATE.rdb

# 备份配置文件
tar -czvf $BACKUP_DIR/config_$DATE.tar.gz ./backend/config ./docker

# 保留最近30天备份
find $BACKUP_DIR -type f -mtime +30 -delete

echo "Backup completed: $DATE"
```

---

## 安全合规最佳实践

### 1. 数据隐私保护

```go
// 敏感数据脱敏
func maskEmail(email string) string {
    if len(email) < 5 {
        return email
    }
    atIndex := strings.Index(email, "@")
    if atIndex == -1 {
        return email
    }
    prefix := email[:2]
    suffix := email[atIndex:]
    return prefix + "****" + suffix
}

func maskIP(ip string) string {
    parts := strings.Split(ip, ".")
    if len(parts) == 4 {
        return parts[0] + "." + parts[1] + ".***.***"
    }
    return ip
}
```

### 2. GDPR合规

```javascript
// Cookie同意管理
class CookieManager {
    constructor() {
        this.consent = this.loadConsent();
    }
    
    loadConsent() {
        const stored = localStorage.getItem('cookie-consent');
        return stored ? JSON.parse(stored) : null;
    }
    
    saveConsent(consent) {
        this.consent = consent;
        localStorage.setItem('cookie-consent', JSON.stringify(consent));
    }
    
    hasAnalyticsConsent() {
        return this.consent?.analytics === true;
    }
    
    hasBehaviorConsent() {
        return this.consent?.behavior === true;
    }
}
```

### 3. 访问控制

```go
// 基于角色的访问控制
func requireRole(role string) http.HandlerFunc {
    return func(w http.ResponseWriter, r *http.Request) {
        userRole := r.Context().Value("user_role").(string)
        
        if userRole != role && userRole != "admin" {
            http.Error(w, "权限不足", http.StatusForbidden)
            return
        }
        
        next.ServeHTTP(w, r)
    }
}

// 使用
http.Handle("/admin/", requireRole("admin")(adminHandler))
http.Handle("/api/", requireRole("user")(apiHandler))
```

### 4. 审计日志

```go
type AuditLog struct {
    ID        string    `json:"id"`
    UserID    string    `json:"user_id"`
    Action    string    `json:"action"`
    Resource  string    `json:"resource"`
    Timestamp time.Time `json:"timestamp"`
    IP        string    `json:"ip"`
    UserAgent string    `json:"user_agent"`
}

func logAudit(userID, action, resource string, r *http.Request) {
    log := AuditLog{
        ID:        uuid.New().String(),
        UserID:    userID,
        Action:    action,
        Resource:  resource,
        Timestamp: time.Now(),
        IP:        getClientIP(r),
        UserAgent: r.UserAgent(),
    }
    
    // 写入审计日志存储
    auditLogStore.Write(log)
}
```

---

## 总结

本最佳实践文档涵盖了HJTPX行为验证系统的各个方面：

1. **安全性**：API密钥管理、请求签名、HTTPS、输入验证
2. **性能**：连接池、缓存、异步处理、限流、数据库优化
3. **可靠性**：重试机制、熔断器、超时处理、健康检查
4. **集成**：验证码类型选择、难度调整、前后端集成、多语言支持
5. **监控运维**：指标收集、日志记录、告警配置、定期备份
6. **安全合规**：数据隐私、GDPR、访问控制、审计日志

遵循这些最佳实践可以帮助您构建安全、高效、可靠的验证码验证系统。