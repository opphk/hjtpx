# 行为验证系统 - 开发核心文档

## 项目概述
开发一个完整的行为验证系统，前后端均使用Go语言，目标是超越极验、易盾、五秒盾等现有产品。

## 核心特性
- 多种验证方式：滑块验证、点选验证、图形验证码、行为分析
- 用户端：简洁易用的验证界面
- 管理端：完整的验证数据统计、配置管理
- 高安全性：AI驱动的行为分析，有效抵御机器攻击

## 技术栈
- 后端：Go + Gin + GORM
- 数据库：PostgreSQL + Redis
- 前端：原生HTML/JavaScript（CSS统一从bootcdn.cn加载）
- UI框架：Bootstrap 5（bootcdn.cn）+ Font Awesome 6（bootcdn.cn）

## 开发规范

### CSS 使用规范（硬性要求）
- **所有 CSS 必须从 `https://www.bootcdn.cn` 加载，禁止编写任何自定义 CSS 代码**
- 允许使用 `<style>` 内联标签编写少量布局辅助样式（如 flex、margin、padding 等 Bootstrap 无法直接覆盖的场景），但不得编写完整的 UI 样式
- 项目中不得存在任何 `.css` 文件
- 优先使用 Bootstrap 5 内置的 utility classes 实现布局和样式

### 已使用的 CDN 链接
| 资源 | CDN 链接 |
|------|---------|
| Bootstrap 5 CSS | `https://cdn.bootcdn.net/ajax/libs/twitter-bootstrap/5.3.8/css/bootstrap.min.css` |
| Bootstrap 5 JS | `https://cdn.bootcdn.net/ajax/libs/twitter-bootstrap/5.3.8/js/bootstrap.bundle.min.js` |
| Font Awesome 6 CSS | `https://cdn.bootcdn.net/ajax/libs/font-awesome/6.5.1/css/all.min.css` |
| Chart.js | `https://cdn.bootcdn.net/ajax/libs/Chart.js/4.4.0/chart.umd.min.js` |

## 项目架构
```
hjtpx/
├── backend/                    # 后端服务
│   ├── cmd/                    # 程序入口
│   │   ├── api/
│   │   └── server/
│   ├── internal/               # 内部代码
│   │   └── api/
│   │       ├── handler/        # API处理器
│   │       ├── middleware/     # 中间件
│   │       ├── router/         # 路由
│   │       └── service/        # 业务逻辑
│   └── pkg/                    # 公共包
│       ├── config/             # 配置
│       ├── database/           # 数据库
│       ├── models/             # 数据模型
│       ├── postgres/           # PostgreSQL
│       ├── redis/              # Redis
│       ├── response/           # 响应
│       └── jwt/                # JWT认证
├── frontend/                   # 前端（用户端，CSS从bootcdn.cn加载）
│   ├── static/
│   │   └── js/
│   └── templates/
├── admin/                      # 管理端（CSS从bootcdn.cn加载，支持CDN/本地切换）
│   ├── static/
│   │   └── js/
│   └── templates/
├── .trae/                      # 规划文档
│   └── specs/
└── 开发核心.md
```

## 开发进度
- [x] 项目初始化
- [x] 数据库设计与搭建
- [x] 后端基础框架搭建
- [x] 用户端API开发（滑块、点选、图形验证码）
- [x] 管理端API开发（认证、统计、应用、日志）
- [x] 验证核心算法开发（行为分析、风险评分）
- [x] 用户端界面开发
- [x] 管理端界面开发
- [ ] 集成测试
- [ ] 部署上线

## API接口文档

### 用户端验证API
- `POST /api/v1/captcha/slider/generate` - 生成滑块验证码
- `POST /api/v1/captcha/slider/verify` - 验证滑块
- `POST /api/v1/captcha/click/generate` - 生成点选验证码
- `POST /api/v1/captcha/click/verify` - 验证点选
- `GET /api/v1/captcha/image` - 生成图形验证码
- `POST /api/v1/captcha/image/verify` - 验证图形验证码

### 管理端API
- `POST /api/v1/auth/login` - 管理员登录
- `POST /api/v1/admin/logout` - 管理员登出
- `GET /api/v1/admin/stats/verification` - 验证统计
- `GET /api/v1/admin/stats/chart` - 图表数据
- `GET /api/v1/admin/applications` - 应用列表
- `POST /api/v1/admin/applications` - 创建应用
- `PUT /api/v1/admin/applications/:id` - 更新应用
- `DELETE /api/v1/admin/applications/:id` - 删除应用
- `GET /api/v1/admin/logs` - 验证日志
- `GET /api/v1/admin/logs/:id` - 日志详情

## 下一步计划
1. 集成测试和bug修复
2. 性能优化
3. 部署上线
