# HJTPX 开发核心文档

## 项目概述

本项目是一个现代化的全栈应用程序，提供 RESTful API 接口服务。

## 技术栈

- 后端框架：Express.js ^4.18.2
- 数据库：PostgreSQL ^8.11.0
- 缓存：Redis ^4.6.0
- 认证：JWT ^9.0.0
- 验证：Joi ^17.11.0
- 限流：express-rate-limit ^7.1.5
- 密码加密：bcrypt ^5.1.1
- 测试：Jest ^29.7.0
- 日志：Winston
- 文件上传：Multer
- 代码格式化：Prettier ^3.0.0
- 代码检查：ESLint ^8.50.0
- Git Hooks：Husky
- CI/CD：GitHub Actions

## 当前进度

- [x] 基础环境配置
- [x] 项目结构搭建
- [x] API接口开发
  - [x] API 版本控制
  - [x] 请求验证中间件
  - [x] 响应格式化中间件
  - [x] 日志中间件
  - [x] 限流中间件
  - [x] CORS 中间件
  - [x] 健康检查路由
  - [x] 认证路由
  - [x] 用户管理路由
  - [x] API 文档
  - [x] API 测试
- [x] 日志和监控系统（大任务 7）
- [x] 通知和消息系统（大任务 6）
- [x] 数据导入导出功能（大任务 5）
- [x] 文件管理和存储（大任务 9）
- [x] 搜索和过滤功能（大任务 8）
- [x] CI/CD 配置（大任务 11）
- [x] 代码质量检查配置（大任务 12）
- [ ] 用户认证系统（部分完成）
- [ ] 前端UI开发
- [ ] 数据库设计
- [ ] 测试覆盖
- [ ] 部署配置

## API 接口开发详情

### 已完成的 API 功能

#### 1. API 版本控制

- 目录：`src/backend/routes/v1/`
- 文件：`src/backend/routes/v1/index.js`
- 功能：
  - 路由版本控制
  - API 路由聚合
  - 端点注册

#### 2. 中间件体系

| 中间件 | 文件路径 | 功能 |
|--------|----------|------|
| 响应格式化 | `src/backend/middleware/responseFormatter.js` | 统一响应格式 |
| 日志记录 | `src/backend/middleware/logger.js` | 请求/响应日志 |
| 请求日志 | `src/backend/middleware/requestLogger.js` | Winston 请求日志 |
| 限流控制 | `src/backend/middleware/rateLimiter.js` | IP/用户级别限流 |
| CORS | `src/backend/middleware/cors.js` | 跨域资源共享 |
| 数据验证 | `src/backend/middleware/validator.js` | Joi 验证 |
| 错误处理 | `src/backend/middleware/errorHandler.js` | 统一错误处理 |
| 文件上传 | `src/backend/middleware/upload.js` | Multer 文件上传 |

#### 3. 验证规则

- 文件：`src/backend/utils/validationRules.js`
- 规则：
  - `userSchema`：用户注册验证
  - `loginSchema`：登录验证
  - `updateUserSchema`：用户更新验证

#### 4. API 端点

**健康检查**

- `GET /api/v1/health` - 基础健康检查
- `GET /api/v1/health/detailed` - 详细健康检查

**认证接口**

- `POST /api/v1/auth/login` - 用户登录
- `POST /api/v1/auth/register` - 用户注册
- `POST /api/v1/auth/verify` - 验证 Token
- `POST /api/v1/auth/refresh` - 刷新 Token
- `POST /api/v1/auth/logout` - 用户登出

**用户管理**

- `GET /api/v1/users` - 获取用户列表
- `GET /api/v1/users/:id` - 获取指定用户
- `POST /api/v1/users` - 创建用户
- `PUT /api/v1/users/:id` - 更新用户
- `DELETE /api/v1/users/:id` - 删除用户

**通知系统**

- `GET /api/v1/notifications` - 获取用户通知列表
- `GET /api/v1/notifications/unread-count` - 获取未读数量
- `GET /api/v1/notifications/:id` - 获取指定通知
- `POST /api/v1/notifications` - 创建通知
- `PUT /api/v1/notifications/:id/read` - 标记为已读
- `PUT /api/v1/notifications/read-all` - 全部标记为已读
- `DELETE /api/v1/notifications/:id` - 删除通知

**数据导出**

- `GET /api/v1/export/:format` - 导出数据（支持 CSV、JSON、Excel）
- `POST /api/v1/export/batch` - 批量导出

**数据导入**

- `POST /api/v1/import/csv` - 导入 CSV 文件
- `POST /api/v1/import/json` - 导入 JSON 文件
- `POST /api/v1/import/preview` - 预览导入数据

**文件管理**

- `POST /api/v1/files/upload` - 上传文件
- `GET /api/v1/files` - 获取文件列表
- `GET /api/v1/files/stats` - 获取存储统计
- `GET /api/v1/files/:id` - 获取文件详情
- `GET /api/v1/files/:id/download` - 下载文件
- `POST /api/v1/files/:id/copy` - 复制文件
- `POST /api/v1/files/:id/move` - 移动文件
- `DELETE /api/v1/files/:id` - 删除文件
- `DELETE /api/v1/files/folder/:folder` - 删除文件夹

**搜索功能**

- `GET /api/v1/search/:model` - 搜索模型数据
- `POST /api/v1/search/:model/advanced` - 高级搜索
- `GET /api/v1/search/:model/suggestions` - 获取搜索建议
- `GET /api/v1/search/:model/fields` - 获取可搜索字段

#### 5. API 文档

- 文件：`src/backend/routes/v1/docs.yaml`
- 格式：OpenAPI 3.0
- 内容：端点定义、请求/响应格式、错误码

#### 6. 测试覆盖

- 文件：`tests/api/health.test.js`
- 测试范围：
  - 健康检查端点
  - 响应时间
  - 错误处理
  - 安全头
  - CORS

## 日志和监控系统

### Winston 日志配置

- 文件：`src/backend/utils/logger.js`
- 功能：
  - 多级别日志（error, warn, info, http, debug）
  - 文件和控制台输出
  - 日志轮转（5MB，最大5个文件）
  - JSON 格式输出
  - 请求日志记录
  - 错误日志追踪

### 请求日志中间件

- 文件：`src/backend/middleware/requestLogger.js`
- 功能：
  - 请求 ID 自动生成
  - 请求/响应时间追踪
  - 敏感数据脱敏
  - 慢请求检测

### 日志配置

- 文件：`src/backend/config/logging.js`
- 环境变量：
  - `LOG_LEVEL`: 日志级别
  - `LOG_CONSOLE`: 启用控制台日志
  - `LOG_FILE`: 启用文件日志
  - `LOG_MAX_SIZE`: 最大日志文件大小
  - `SLOW_REQUEST_THRESHOLD`: 慢请求阈值

### 监控端点

- `GET /api/v1/health/detailed` - 详细健康检查
- 包含：数据库连接、Redis 连接、系统资源

## 通知和消息系统

### 通知模型

- 文件：`src/backend/models/Notification.js`
- 功能：
  - 通知类型（info, success, warning, error, system, message, reminder, alert）
  - 优先级（low, normal, high, urgent）
  - 状态（unread, read, archived）
  - 多渠道支持（in_app, email, sms, push）
  - 自动过期（TTL）

### 通知服务

- 文件：`src/backend/services/notificationService.js`
- 功能：
  - 创建单个/批量通知
  - 获取通知列表（分页）
  - 获取未读数量
  - 标记已读/全部已读
  - 删除通知
  - 自动归档旧通知

### 通知铃铛组件

- 文件：`src/frontend/components/NotificationBell.jsx`
- 功能：
  - 未读数量显示
  - 通知列表下拉
  - 单个/全部标记已读
  - 自动刷新（30秒）
  - 类型图标显示

## 数据导入导出功能

### 导出服务

- 文件：`src/backend/services/exportService.js`
- 支持格式：
  - CSV：使用 json2csv 库
  - JSON：原生 JSON.stringify
  - Excel：使用 ExcelJS 库
- 功能：
  - 字段选择
  - 记录数限制（10000条）
  - 文件名自定义
  - 批量导出

### 导入服务

- 文件：`src/backend/services/importService.js`
- 支持格式：
  - CSV：使用 csv-parse/sync
  - JSON：原生 JSON.parse
- 功能：
  - 数据验证
  - 批量插入
  - 进度回调
  - 错误报告
  - 记录数限制（5000条）

### 文件验证

- 文件：`src/backend/utils/fileValidator.js`
- 功能：
  - 文件扩展名验证
  - MIME 类型检测（使用 file-type）
  - 文件大小限制
  - 文件名清理

## 文件管理和存储

### 文件服务

- 文件：`src/backend/services/fileService.js`
- 功能：
  - 文件上传（支持批量）
  - 文件下载
  - 文件复制/移动
  - 文件夹管理
  - 存储统计
  - 文件记录持久化

### 存储配置

- 文件：`config/storage.js`
- 支持存储：
  - 本地存储
  - Amazon S3
  - Google Cloud Storage
  - Azure Blob Storage
- 功能：
  - 图片处理（缩放、压缩、缩略图）
  - CDN 集成
  - 自动清理

### 上传中间件

- 文件：`src/backend/middleware/upload.js`
- 功能：
  - Multer 配置
  - 文件类型过滤
  - 大小限制
  - 批量上传

## 搜索和过滤功能

### 搜索服务

- 文件：`src/backend/services/searchService.js`
- 功能：
  - 全文搜索
  - 过滤器（eq, ne, gt, gte, lt, lte, in, nin, like, between）
  - 排序（多字段）
  - 分页
  - 查询性能追踪
  - 搜索建议

### 搜索优化

- 文件：`src/backend/utils/searchOptimizer.js`
- 功能：
  - 查询复杂度分析
  - 过滤器顺序优化
  - 结果缓存（TTL 300秒）
  - 慢查询检测
  - 性能建议

### 搜索栏组件

- 文件：`src/frontend/components/SearchBar.jsx`
- 功能：
  - 实时搜索（防抖 300ms）
  - 搜索建议
  - 过滤器下拉
  - 结果点击
  - 加载更多

## CI/CD 配置

### CI 工作流

- 文件：`.github/workflows/ci.yml`
- 触发条件：
  - Push 到 main、develop 分支
  - Pull Request
- 阶段：
  - Lint：ESLint + Prettier
  - Test：单元测试 + 集成测试
  - Security：npm audit + Snyk
  - Build：应用构建
  - E2E：端到端测试（仅 PR）
  - Notify：Slack 通知

### 部署工作流

- 文件：`.github/workflows/deploy.yml`
- 触发条件：
  - Push 到 main 分支
  - 手动触发
- 环境：
  - Staging：自动部署
  - Production：需审批
- 阶段：
  - Prepare：环境准备
  - Build：Docker 镜像构建
  - Test：部署前测试
  - Deploy：部署到 ECS
  - Rollback：失败自动回滚

## 代码质量检查配置

### ESLint 配置

- 文件：`.eslintrc.js`
- 插件：
  - react、react-hooks、jsx-a11y
  - import、prettier
- 规则：
  - Prettier 格式化强制
  - React prop-types 警告
  - Import 排序
  - 最佳实践规则

### Prettier 配置

- 文件：`.prettierrc`
- 规则：
  - 单引号
  - 尾部逗号：无
  - 打印宽度：100
  - Tab 宽度：2

### Git Hooks (Husky)

- 文件：`.husky/`
- Hooks：
  - pre-commit：lint-staged
  - commit-msg：Conventional Commits 验证

### VS Code 配置

- 文件：`.vscode/settings.json`
- 功能：
  - 保存时自动格式化
  - ESLint 自动修复
  - 推荐的扩展

### 代码质量文档

- 文件：`docs/code-quality/guide.md`
- 内容：
  - 代码风格指南
  - ESLint/Prettier 使用
  - Git Hooks 配置
  - Conventional Commits 规范
  - 测试要求
  - 安全最佳实践

## 测试统计

### 测试结果汇总 (2026-05-14)

| 指标 | 数值 |
|------|------|
| 测试套件总数 | 9 |
| 通过测试套件 | 3 |
| 失败测试套件 | 6 |
| 测试用例总数 | 104 |
| 通过测试用例 | 81 |
| 失败测试用例 | 23 |

### 覆盖率报告

| 指标 | 当前覆盖率 | 目标覆盖率 | 状态 |
|------|-----------|-----------|------|
| 语句覆盖率 | 24.91% | 80% | ❌ 不达标 |
| 分支覆盖率 | 17.36% | 80% | ❌ 不达标 |
| 函数覆盖率 | 22.91% | 80% | ❌ 不达标 |
| 行覆盖率 | 25.51% | 80% | ❌ 不达标 |

### 失败测试详情

#### 1. 数据库连接测试 (connection.test.js)
- 原因: PostgreSQL 密码认证失败
- 影响: 15 个测试失败

#### 2. 认证测试 (auth.test.js)
- `should generate valid JWT token`: token 返回 undefined
- `should reject request with invalid token`: 未正确调用 401
- `should allow request with valid token`: jwt 未定义
- `should reject malformed authorization header`: 未正确调用 401

#### 3. 基础测试 (basic.test.js)
- `should handle API health check`: 服务器未运行 (ECONNREFUSED)

#### 4. 前端测试 (useAuth.test.js)
- Jest 配置问题: 缺少 @babel/preset-react
- 语法错误: JSX 不支持

#### 5. API 测试 (health.test.js)
- 找不到模块: '../../../src/index'

### 代码质量检查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| ESLint | ⚠️ 通过 | 配置完善 |
| Prettier | ⚠️ 通过 | 配置完善 |
| Husky | ⚠️ 通过 | Git Hooks 已配置 |

### 改进建议

1. **测试环境**: 配置数据库连接或使用内存数据库
2. **Jest 配置**: 添加 @babel/preset-react 支持 JSX
3. **模块路径**: 修复 tests/api/health.test.js 中的导入路径
4. **覆盖率**: 增加测试用例以达到 80% 目标
5. **新功能测试**: 为新增模块编写测试

## 项目结构

```
src/
├── index.js                          # 应用入口
├── frontend/
│   ├── components/                   # React 组件
│   │   ├── NotificationBell.jsx     # 通知铃铛
│   │   └── SearchBar.jsx            # 搜索栏
│   ├── App.jsx                      # 应用主组件
│   └── index.js                      # 前端入口
└── backend/
    ├── config/
    │   ├── database/                 # 数据库配置
    │   ├── logging.js               # 日志配置
    │   └── storage.js               # 存储配置
    ├── middleware/                   # 中间件
    │   ├── auth.js                   # 认证中间件
    │   ├── cors.js                   # CORS 配置
    │   ├── errorHandler.js           # 错误处理
    │   ├── logger.js                 # 日志记录
    │   ├── requestLogger.js          # Winston 请求日志
    │   ├── rateLimiter.js           # 限流控制
    │   ├── responseFormatter.js      # 响应格式化
    │   ├── upload.js                 # 文件上传
    │   └── validator.js              # 数据验证
    ├── models/
    │   └── Notification.js           # 通知模型
    ├── routes/                       # 路由定义
    │   ├── v1/                       # API v1 版本
    │   │   ├── index.js             # 路由聚合
    │   │   ├── health.js            # 健康检查
    │   │   ├── users.js             # 用户管理
    │   │   ├── auth.js              # 认证接口
    │   │   └── docs.yaml            # API 文档
    │   ├── notifications.js         # 通知路由
    │   ├── export.js                 # 导出路由
    │   ├── import.js                 # 导入路由
    │   ├── files.js                  # 文件路由
    │   └── search.js                 # 搜索路由
    ├── services/                     # 业务逻辑
    │   ├── authService.js           # 认证服务
    │   ├── userService.js           # 用户服务
    │   ├── notificationService.js   # 通知服务
    │   ├── exportService.js         # 导出服务
    │   ├── importService.js         # 导入服务
    │   ├── fileService.js           # 文件服务
    │   └── searchService.js         # 搜索服务
    └── utils/                        # 工具函数
        ├── validationRules.js        # 验证规则
        ├── logger.js                 # Winston 日志
        ├── fileValidator.js         # 文件验证
        ├── queryOptimizer.js        # 查询优化
        └── searchOptimizer.js       # 搜索优化
.github/
├── workflows/
│   ├── ci.yml                       # CI 工作流
│   └── deploy.yml                   # 部署工作流
.husky/
├── pre-commit                       # Pre-commit hook
└── commit-msg                       # Commit-msg hook
.vscode/
├── settings.json                    # VS Code 配置
└── recommendedExtensions.code-snippets
docs/
├── database/
│   └── schema.md                    # 数据库架构
├── monitoring/
│   └── guide.md                     # 监控指南
├── storage/
│   └── guide.md                     # 存储指南
├── ci-cd/
│   └── pipeline.md                  # CI/CD 文档
├── code-quality/
│   └── guide.md                     # 代码质量指南
└── testing/
    └── guide.md                     # 测试指南
migrations/
├── 001_initial_schema.sql           # 初始架构
├── 002_add_roles.sql               # 添加角色
├── 003_create_indexes.sql          # 创建索引
└── 004_create_notifications.sql    # 通知表
config/
├── .env.example                     # 环境变量示例
└── storage.js                       # 存储配置
```

## 开发计划

### 第一阶段（已完成）

1. 环境配置和依赖安装
2. 项目结构设计
3. 基础中间件开发
4. API 版本控制
5. 健康检查接口
6. 认证接口
7. 用户管理接口

### 第二阶段（已完成）

1. 日志和监控系统
   - Winston 日志配置
   - 请求日志中间件
   - 日志配置管理
   - 监控指南文档

2. 通知和消息系统
   - 通知模型（MongoDB）
   - 通知路由和服务
   - 通知铃铛组件
   - 通知表迁移

3. 数据导入导出功能
   - 导出服务（CSV、JSON、Excel）
   - 导入服务
   - 文件验证工具
   - 导入导出路由

4. 文件管理和存储
   - 文件服务
   - 文件路由
   - 上传中间件
   - 存储配置
   - 存储指南

5. 搜索和过滤功能
   - 搜索服务
   - 搜索路由
   - 搜索优化工具
   - 搜索栏组件

6. CI/CD 配置
   - CI 工作流
   - 部署工作流
   - CI/CD 文档

7. 代码质量检查
   - 完善 ESLint 配置
   - 完善 Prettier 配置
   - Husky Git Hooks
   - VS Code 配置
   - 代码质量指南

### 第三阶段（待开发）

1. 前端界面开发
2. 业务逻辑开发
3. 性能优化
4. 安全加固
5. 部署上线

## 代码规范

- 使用 ESLint 进行代码检查
- 使用 Prettier 进行代码格式化
- 遵循 Conventional Commits 规范
- 所有新功能必须包含测试
- 使用英语注释，文档可使用中文
- Git Hooks 自动检查（pre-commit、commit-msg）

## 测试要求

- 单元测试覆盖率 > 80%
- 集成测试覆盖主要业务流程
- E2E 测试覆盖关键用户路径

### 运行测试

```bash
# 运行所有测试
npm test

# 运行测试覆盖率
npm run test:coverage

# 运行单元测试
npm run test:unit

# 运行 API 测试
npm run test:api

# 监听模式运行测试
npm run test:watch
```

## 安全特性

### 已实现

1. **JWT 认证**
   - Token 生成和验证
   - Token 刷新机制
   - 过期时间控制

2. **密码加密**
   - bcrypt 哈希存储
   - 强度验证

3. **限流保护**
   - IP 级别限流
   - 用户级别限流
   - 认证限流

4. **输入验证**
   - Joi 数据验证
   - SQL 注入防护
   - XSS 防护

5. **CORS 配置**
   - 跨域请求支持
   - 凭证传递
   - 来源白名单

6. **文件上传安全**
   - 文件类型验证
   - 文件大小限制
   - MIME 类型检测
   - 文件名清理

7. **代码质量**
   - ESLint 代码检查
   - Prettier 格式化
   - Git Hooks 验证

### 待实现

1. 速率限制优化
2. API 密钥认证
3. 审计日志
4. 数据加密

## 环境变量

### 必需变量

| 变量名 | 说明 | 示例 |
|--------|------|------|
| PORT | 服务端口 | 3000 |
| NODE_ENV | 运行环境 | development / production |
| JWT_SECRET | JWT 密钥 | your-secret-key |
| JWT_EXPIRES_IN | Token 有效期 | 7d |

### 数据库变量

| 变量名 | 说明 | 示例 |
|--------|------|------|
| DB_HOST | 数据库主机 | localhost |
| DB_PORT | 数据库端口 | 5432 |
| DB_NAME | 数据库名称 | hjtpx |
| DB_USER | 数据库用户 | postgres |
| DB_PASSWORD | 数据库密码 | password |

### Redis 变量

| 变量名 | 说明 | 示例 |
|--------|------|------|
| REDIS_HOST | Redis 主机 | localhost |
| REDIS_PORT | Redis 端口 | 6379 |
| REDIS_PASSWORD | Redis 密码 | password |

### 日志变量

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| LOG_LEVEL | 日志级别 | info |
| LOG_CONSOLE | 启用控制台日志 | true |
| LOG_FILE | 启用文件日志 | false |
| LOG_MAX_SIZE | 最大日志大小 | 5242880 |
| SLOW_REQUEST_THRESHOLD | 慢请求阈值(ms) | 3000 |

### 存储变量

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| STORAGE_PROVIDER | 存储提供者 | local |
| LOCAL_UPLOAD_DIR | 本地上传目录 | ./uploads |
| LOCAL_MAX_FILE_SIZE | 最大文件大小 | 10485760 |
| CDN_ENABLED | 启用 CDN | false |
| CDN_URL | CDN URL | - |

## 维护说明

### 日志管理

- Winston 结构化日志输出
- 多级别支持（error, warn, info, http, debug）
- 日志文件轮转（生产环境）
- 请求 ID 追踪
- 敏感数据脱敏

### 性能监控

- 监控 API 响应时间
- 慢请求检测和警告
- 查询性能分析
- 数据库连接状态
- Redis 连接状态
- 内存和 CPU 使用情况

### 故障排查

1. 检查健康检查端点状态
2. 查看应用日志（JSON 格式）
3. 检查数据库连接
4. 检查 Redis 连接
5. 验证环境变量配置
6. 检查慢请求日志

## 更新日志

### v1.1.0 (2026-05-14)

#### 新增功能

**日志和监控系统**
- Winston 日志配置（`src/backend/utils/logger.js`）
- 请求日志中间件（`src/backend/middleware/requestLogger.js`）
- 日志配置（`src/backend/config/logging.js`）
- 监控指南（`docs/monitoring/guide.md`）

**通知和消息系统**
- 通知模型（`src/backend/models/Notification.js`）
- 通知路由（`src/backend/routes/notifications.js`）
- 通知服务（`src/backend/services/notificationService.js`）
- 通知迁移（`migrations/004_create_notifications.sql`）
- 通知铃铛组件（`src/frontend/components/NotificationBell.jsx`）

**数据导入导出功能**
- 导出服务（`src/backend/services/exportService.js`）
- 导入服务（`src/backend/services/importService.js`）
- 导出路由（`src/backend/routes/export.js`）
- 导入路由（`src/backend/routes/import.js`）
- 文件验证工具（`src/backend/utils/fileValidator.js`）

**文件管理和存储**
- 文件服务（`src/backend/services/fileService.js`）
- 文件路由（`src/backend/routes/files.js`）
- 上传中间件（`src/backend/middleware/upload.js`）
- 存储配置（`config/storage.js`）
- 存储指南（`docs/storage/guide.md`）

**搜索和过滤功能**
- 搜索服务（`src/backend/services/searchService.js`）
- 搜索路由（`src/backend/routes/search.js`）
- 搜索优化工具（`src/backend/utils/searchOptimizer.js`）
- 搜索栏组件（`src/frontend/components/SearchBar.jsx`）

**CI/CD 配置**
- CI 工作流（`.github/workflows/ci.yml`）
- 部署工作流（`.github/workflows/deploy.yml`）
- CI/CD 文档（`docs/ci-cd/pipeline.md`）

**代码质量检查配置**
- 完善 ESLint 配置（`.eslintrc.js`）
- 完善 Prettier 配置（`.prettierrc`）
- Husky Git Hooks（`.husky/pre-commit`, `.husky/commit-msg`）
- VS Code 配置（`.vscode/settings.json`）
- Lint-staged 配置（`.lintstagedrc`）
- 代码质量指南（`docs/code-quality/guide.md`）

#### 优化项

- 完善的 ESLint 规则集
- Prettier 文件类型覆盖
- Git Hooks 自动化
- 代码质量检查集成

### v1.0.0 (2026-05-14)

#### 新增功能

- API 版本控制系统
- 响应格式化中间件
- 日志记录中间件
- 限流中间件（支持 Redis）
- CORS 中间件
- 数据验证中间件（Joi）
- 健康检查接口（基础 + 详细）
- 认证接口（登录、注册、验证、刷新、登出）
- 用户管理接口（CRUD）
- API 文档（OpenAPI 3.0）
- 健康检查测试

#### 优化项

- 统一的错误处理
- 安全的密码存储
- 跨域请求支持
- 请求 ID 追踪
- 进程异常处理
