(function($) {
    'use strict';
    
    $(document).ready(function() {
        if (typeof CaptchaX === 'undefined') {
            console.warn('CaptchaX SDK not loaded');
            return;
        }
    });
    
})(jQuery);
